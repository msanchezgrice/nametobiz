{
  "installCommand": "npm install",
  "buildCommand": "cd packages/utils && npm install && npm run build && cd ../db && npm install && npm run build && cd ../llm && npm install && npm run build && cd ../../apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/.next"
}